{
  "name": "nickkampe-portfolio",
  "version": "1.0.0",
  "description": "Nick Kampe's platform engineering and infrastructure portfolio",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "e2e"
  ],
  "scripts": {
    "dev": "bun run --parallel dev:frontend dev:backend",
    "dev:frontend": "cd frontend && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "build": "bun run build:frontend && bun run build:backend",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "cd backend && bun run build",
    "start": "cd backend && bun run start",
    "test": "bun run test:frontend && bun run test:backend",
    "test:frontend": "cd frontend && bun run test",
    "test:backend": "cd backend && bun run test",
    "test:e2e": "bun run build && cd e2e && bun run test",
    "test:all": "bun run test && bun run test:e2e",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "portfolio",
    "generative-art",
    "devops",
    "sre",
    "three.js",
    "vue3",
    "bun"
  ],
  "author": "Nicholas Kampe",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "overrides": {
    "flatted": "^3.4.4",
    "picomatch": "^4.0.7"
  }
}
